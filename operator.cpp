#include<iostream>
using namespace  std;

int main(){
    int a = 2;
    int b = 3;

    // bitwise operator (works in 1 and 0's only)
    // cout<<"this the and function"<<endl;
    // cout<<(a & b)<<endl;
    // cout<<(a|b)<<endl;
    // cout<<~a<<endl;;
    // cout<<(a^b)<<endl;

    // left shift operator (<<) to a b steps on left side
    // cout<< (15555<<1);

    // right shift
    // cout<< (5>>2);
    // padding for positive number always start's from 0 but for negative it depends on complier
    
}